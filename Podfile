# Podfile
use_frameworks!

target 'WeatherHelper' do
    pod 'Alamofire', '~> 5.0.0-beta.5'
    pod 'HandyJSON', '~> 5.0.0-beta.1'
end

# RxTests 和 RxBlocking 将在单元/集成测试中起到重要作用
target 'WeatherHelperTests' do

end
